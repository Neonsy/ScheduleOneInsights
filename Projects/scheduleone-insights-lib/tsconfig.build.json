// tsconfig.build.json
{
    "extends": "./tsconfig.dev.json",
    "compilerOptions": {
        "noEmit": false, // We want to emit when building
        "outDir": "./dist",
        "rootDir": "src"
    },
    "include": ["src/**/*"],
    "exclude": ["node_modules", "dist", "tests"]
}
