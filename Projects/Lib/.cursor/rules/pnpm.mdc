---
description: 
globs: 
alwaysApply: true
---
Dependency Management

use-pnpm

Always use pnpm for installing, adding, and removing dependencies as well as other node commands.

update-lockfile

After adding/removing deps, run pnpm install to update the lockfile.
